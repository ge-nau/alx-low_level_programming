#include <stdio.h>
#include "main.h"

/**
 * _isdigit - checks for digits
 * @c: parameter
 * Return: 1 if digit  otherwise 0
 */

int _isdigit(int c)
{
	if (c >= 48 && c < 58)
	{
		returb (1);
	}
	else
	{
		return (0);
	}
}
